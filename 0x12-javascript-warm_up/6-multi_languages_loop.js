#!/usr/bin/node

// this sccript prints element froma loop

const strings = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < strings.length; i++) {
  console.log(strings[i]);
}
